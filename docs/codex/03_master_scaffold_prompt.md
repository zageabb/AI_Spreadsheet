# Master Scaffold Prompt

```text
Use the repository https://github.com/zageabb/AI_Spreadsheet as the target project.

Read AGENTS.md and docs/tasks.md first.

The repository is currently only a starter, so begin by creating the full project scaffold for a Python desktop spreadsheet application.

First objective:
Build the repository foundation and working MVP skeleton only.

Do not try to finish every advanced feature in one pass.

Implement:
- project folder structure
- requirements.txt
- README.md
- .gitignore
- .env.example
- Python package layout
- desktop application shell using PySide6
- main window with menu bar, toolbar, formula bar, worksheet grid, sheet tabs, and status bar
- workbook, worksheet, and cell data models
- JSON storage adapter
- sample workbook JSON
- starter formula engine scaffold
- plugin/custom function scaffold
- placeholder folders/modules for PostgreSQL, auth, permissions, collaboration server, and email service

Keep the architecture modular.
Do not use a monolithic script.
Mark scaffolded items clearly.
Output the project tree first, then generate files.
```
