# UI Improvements Prompt

```text
Read AGENTS.md and docs/tasks.md.
Execute this stage only.

Now improve the desktop UI implementation without changing the overall architecture.

Goals:
- make the spreadsheet app feel more like a desktop spreadsheet tool
- improve layout and usability
- keep the code modular

Tasks:
1. Refine the main window layout
2. Improve the menu bar and toolbar/ribbon structure
3. Add a clearer formula bar
4. Improve worksheet grid rendering and editing behaviour
5. Improve row/column header display
6. Improve sheet tab handling
7. Improve the status bar
8. Add keyboard shortcuts where practical
9. Improve basic copy/paste handling
10. Improve undo/redo scaffolding

Requirements:
- keep compatibility with Windows, macOS Sonoma, and Ubuntu 24.04
- do not rewrite the app into a web app
- preserve the existing folder structure where possible
- keep the UI code separated from the spreadsheet engine
- update the README only where needed

Output:
- updated project tree only if changed
- updated files only
- a short note explaining the UI improvements
```
